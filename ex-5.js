// Exercise #5 Min Function
// Function Declaration ที่ใช้ Math.min เพื่อหาค่าที่น้อยที่สุดระหว่าง a และ b:
function findMinNumber(a, b) {
    return Math.min(a, b);
}

const minNumber = findMinNumber(10, 20);

console.log(minNumber); // 10
